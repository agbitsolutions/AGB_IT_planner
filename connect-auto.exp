#!/usr/bin/expect -f
# Automated SSH connection with password
# Note: This stores password in plaintext - use SSH keys for production!

set timeout 20
set password "Agbitsolutions@143"

spawn ssh -p 65002 u544547223@89.117.157.109

expect {
    "password:" {
        send "$password\r"
        interact
    }
    "yes/no" {
        send "yes\r"
        expect "password:"
        send "$password\r"
        interact
    }
}
